package mangroves_types

import rl "vendor:raylib"
import mu "vendor:microui"


SystemDialogUpdate :: proc(system: ^System, ctx : ^mu.Context)
SystemDialogDraw :: proc(system: ^System, ctx : ^mu.Context)

